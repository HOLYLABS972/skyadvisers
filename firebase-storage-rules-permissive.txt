// PERMISSIVE Firebase Storage Rules for Testing
// Use these rules ONLY for development/testing
// DO NOT use in production!

rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow all read and write access (ONLY FOR TESTING)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}

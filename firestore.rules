rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules for testing - allow all read/write operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
